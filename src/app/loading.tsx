import LoadingAnimation from "@/components/animated-illustrations/loading-animation";

const Loading = () => {
  return (
    <div className="h-screen flex items-center justify-center bg-bg-purple">
      <LoadingAnimation />
    </div>
  );
};

export default Loading;
